<script>
  tailwind.config = {
    theme: {
      extend: {
        colors: {
          primary: '#FF6B35',
          'primary-dark': '#cc552a',
          sage: '#7D8E7E',
          'sage-dark': '#2C3E30',
          gold: '#D4AF37',
          bg: '#F5F5F1',
        },
        fontFamily: {
          sans: ['Manrope', 'sans-serif'],
          display: ['Manrope', 'sans-serif'],
        },
        boxShadow: {
          'soft': '0 4px 20px rgba(0, 0, 0, 0.05)',
          'layered': '0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -2px rgba(0,0,0,0.05), 0 20px 25px -5px rgba(0,0,0,0.1)',
        }
      }
    }
  }
</script>

<style>
  :root {
    --color-stitch-primary: #FF6B35;
    --color-stitch-sage: #7D8E7E;
    --color-stitch-bg: #F5F5F1;
  }

  .material-symbols-outlined {
    font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
    vertical-align: middle;
  }

  .icon-filled {
    font-variation-settings: 'FILL' 1;
  }

  /* Stitch Custom Utilities */
  .bg-paper {
    background-color: #F5F5F1;
    background-image: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100' height='100' filter='url(%23noise)' opacity='0.08'/%3E%3C/svg%3E");
  }

  /* Overwrite Dawn's aggressive styles for Stitch sections */
  .stitch-section h1, .stitch-section h2, .stitch-section h3 {
    text-transform: none !important;
    letter-spacing: -0.025em !important;
  }

  /* Progress Bar Animation */
  @keyframes progress-shine {
    from { background-position: 200% 0; }
    to { background-position: -200% 0; }
  }
  .animate-progress {
    background: linear-gradient(90deg, #FF6B35, #ffd166, #FF6B35);
    background-size: 200% 100%;
    animation: progress-shine 3s linear infinite;
  }
</style>
